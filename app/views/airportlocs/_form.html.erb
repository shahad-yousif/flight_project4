<%= form_for(@airportloc) do |f| %>
  <% if @airportloc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airportloc.errors.count, "error") %> prohibited this airportloc from being saved:</h2>

      <ul>
      <% @airportloc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
